# Carry-over of scripts/node-image/node-adapt.sh step (3): k8s nodes exhaust the kernel
# defaults (128 instances / 8192 watches) — kubelet, log tailers and operators all hold watches.
fs.inotify.max_user_instances = 8192
fs.inotify.max_user_watches = 524288
