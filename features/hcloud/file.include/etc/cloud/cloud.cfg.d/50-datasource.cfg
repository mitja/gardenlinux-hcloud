# Hetzner first (DMI sys_vendor=Hetzner on every hcloud VM), NoCloud kept for local QEMU
# smoke tests (seed ISO labelled `cidata`), None as terminal fallback so first boot never
# hangs on datasource discovery. Rationale: features/hcloud/README.md.
datasource_list: [ Hetzner, NoCloud, None ]
